- Verified production `/api/support-chat-webhook` on fichasonline.uy returns `400 Invalid chat session` for an invalid POST, which implies `N8N_SUPPORT_CHAT_WEBHOOK_URL` is configured in production. Local `.env` does not include that variable.
