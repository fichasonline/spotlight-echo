- Francisco wants an authenticated/private admin page for Instagram giveaways on Fichas.uy: paste an Instagram post URL, read all comments, filter entrants, and pick one random winner.
- Treat the Meta token shared in chat as secret and do not echo or hardcode it. It should live in server-side env only, ideally rotated before production because it was pasted into chat.
- Meta docs checked on 2026-06-09: IG media comments are accessible for media created by the token owner with `instagram_basic` + `instagram_manage_comments` (and sometimes page/business permissions such as `pages_read_engagement`/`business_management`). IG User exposes follower counts but not a follower list; `/ig-user/followers` is not an official endpoint. Automatic "does this commenter follow us?" verification is not directly available through the official Instagram Graph API.
- Giveaway rule updated: each eligible Instagram comment is one chance; do not deduplicate by username. The admin UI should show loading progress as comments are paginated from Meta.
