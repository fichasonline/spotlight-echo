- En el admin de noticias se bloqueó el cierre accidental del editor por clic fuera del sheet.
- Se agregó ajuste manual de encuadre de portada para noticias: `image_position_x` e `image_position_y` en `articles`, sliders en `/admin/noticias`, y aplicación del mismo `object-position` en editor, detalle, listado y home para que el preview sea fiel a la publicación.
