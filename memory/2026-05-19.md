- Se empezó a formalizar `social-cm` para publicación de Instagram: migración con `social_posts`, `social_assets` y `social_publish_jobs`; `/api/publish` ahora acepta `type: "social_post"` y puede crear post+assets+job. Francisco aclaró que el publisher real debe vivir en Open Cloud/OpenClaw con acceso directo a Supabase y Meta, no como cron/worker de la web; la app solo define el contrato de datos/cola. Stories siguen fuera del flujo automatizado.
