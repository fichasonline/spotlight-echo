- Conectado el chat de la home de Fichas Online con n8n mediante `/api/support-chat-webhook`: el widget guarda mensajes en Supabase, el endpoint valida `threadId` + `visitorToken`, manda datos/contacto/historial reciente a n8n y persiste la respuesta como mensaje de soporte.
- Variables necesarias para producción: `N8N_SUPPORT_CHAT_WEBHOOK_URL`, `SUPABASE_URL` y `SUPABASE_SERVICE_ROLE_KEY`. Si se cambian localmente, reiniciar Vite para que el servidor las tome.

- 2026-06-18: Added n8n support chat webhook integration fixes: endpoint now sends only latest message, parses n8n replies, and added Supabase RPC migration for automated staff replies to avoid RLS direct insert failures.
