- En `/admin/noticias/instagram` se reorganizó la vista en pestañas: cola, generador e historial, para que el historial no quede mezclado en la misma pantalla principal.
- Se corrigió el generador de stories para que el selector use solo `instagramPendingArticles`; las noticias ya marcadas como publicadas quedan en historial y no aparecen en la cola para generar.
