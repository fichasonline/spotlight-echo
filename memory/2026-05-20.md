- Se agregó configuración admin de liveblogs para social CM: migración `social_sources` + `social_ingest_state` con RLS solo admin usando `public.is_admin(auth.uid())`; nueva pantalla `/admin/liveblogs` para listar, crear URL, toggle enabled y configurar `max_stories_per_hour`. El preview/test queda pendiente para una etapa posterior.
- Se agregó `/admin/stories` para aprobar stories generadas: consulta `social_posts` format story en `draft`/`needs_approval`, muestra primer asset como preview, texto desde `metadata.text`/`metadata.caption` o `caption`, y al publicar actualiza status `queued` + inserta `social_publish_jobs`. También se agregó migración para permitir status `draft` en `social_posts`.
