- Se revisó un fallo de build en Vercel: `ERR_PNPM_OUTDATED_LOCKFILE` porque `package.json` pedía `motion@^12.38.0` y `pnpm-lock.yaml` seguía con `^12.34.2`. Se regeneró el lockfile con `pnpm 10.28.0`; `CI=true pnpm install --frozen-lockfile` y `npm run build` pasan.
