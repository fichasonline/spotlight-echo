- Se corrigió el generador de stories de `/admin/noticias/instagram` para que las portadas externas de noticias puedan usarse en el canvas exportable: ahora `article-story.ts` enruta imágenes externas por `/api/image-proxy`, evitando bloqueos de CORS al crear el PNG de Instagram.
- Se agregó `api/image-proxy.ts` para servir imágenes remotas con headers exportables y una versión equivalente en el dev server de Vite. Tests focalizados de `article-story` y `npm run build` pasan; `npm run lint` sigue fallando por errores preexistentes en archivos no relacionados.
