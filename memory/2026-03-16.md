- Se ajustó `/admin/leads` para mostrar solo leads no originados en `chat_widget` y se alineó el contador de leads del dashboard con ese mismo criterio.
- Se creó `/admin/chat-leads` para listar todos los leads de chat y se agregó una tarjeta específica en el dashboard para acceder a esa vista.
